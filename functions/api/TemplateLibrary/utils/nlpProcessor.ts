export function nlpProcessText(inputData: { careerDetails: string; industry: string }) {
  // Mock NLP processing logic
  const processedData = { ...inputData, keywords: ['example', 'keywords'] };
  return processedData;
}